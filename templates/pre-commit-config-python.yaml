# Python project pre-commit config template
# Copy to your project as .pre-commit-config.yaml

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.3
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ['--maxkb=500']
      - id: check-case-conflict
      - id: mixed-line-ending
      - id: detect-private-key
      - id: check-ast
      - id: check-docstring-first
      - id: debug-statements

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks

  # Optional: uncomment if using Jupyter notebooks
  # - repo: https://github.com/kynan/nbstripout
  #   rev: 0.8.1
  #   hooks:
  #     - id: nbstripout

  # Optional: uncomment if using GitHub Actions
  # - repo: https://github.com/rhysd/actionlint
  #   rev: v1.7.7
  #   hooks:
  #     - id: actionlint

  # Optional: uncomment if using uv for dependency management
  # - repo: https://github.com/astral-sh/uv-pre-commit
  #   rev: 0.5.14
  #   hooks:
  #     - id: uv-lock
