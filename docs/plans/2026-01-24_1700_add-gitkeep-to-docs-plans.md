# Plan: Add .gitkeep to docs/plans

## Task
Ensure `docs/plans/` is tracked in git by adding a `.gitkeep` file.

## Steps
1. Create `docs/plans/.gitkeep` (empty file)

## Verification
- `ls docs/plans/.gitkeep` shows the file exists

## Files
- `docs/plans/.gitkeep` (new)
